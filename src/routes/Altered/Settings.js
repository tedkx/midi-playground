import React from 'react';
import { Card } from 'antd';
import MidiContext from 'components/Midi/Context';

const AlteredSettings = props => {
  return <Card bordered={false}></Card>;
};

export default AlteredSettings;
